# .gitignore 파일 내용

# 1. 데이터 절대 금지 (용량 문제)
data/
*.mp4
*.avi
*.json
*.npy

# 2. 라이브러리 폴더 제외 (각자 설치할 것)
mmpose/

# 3. 가상환경 및 설정 파일 제외
venv/
env/
.idea/
.vscode/
__pycache__/
*.pyc

# 4. 모델 파일 (용량이 크면 제외, 작으면 포함해도 됨)
# 일단 pth 파일은 용량이 크니 제외하고 드라이브로 공유 추천
models/*.pth